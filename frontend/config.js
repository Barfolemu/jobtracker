// Set this to the ApiUrl output from `sam deploy` (or your local `sam local start-api` URL).
window.JOBTRACKER_CONFIG = {
  API_BASE_URL: "https://uku9sxcbl9.execute-api.us-east-1.amazonaws.com",
};
